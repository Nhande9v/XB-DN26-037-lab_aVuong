apiVersion: apps/v1
kind: Deployment
metadata:
  name: good-replicas
  namespace: demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: good-replicas
  template:
    metadata:
      labels:
        app: good-replicas
        owner: platform-team
    spec:
      containers:
        - name: app
          image: nginx:1.27.0
          resources:
            limits:
              cpu: "100m"
              memory: "128Mi"
          securityContext:
            runAsUser: 1000