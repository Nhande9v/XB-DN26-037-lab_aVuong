apiVersion: v1
kind: Pod
metadata:
  name: bad-hostnetwork
  namespace: demo
spec:
  hostNetwork: true
  containers:
    - name: app
      image: nginx:1.27.0
      resources:
        limits:
          cpu: "100m"
          memory: "128Mi"
      securityContext:
        runAsUser: 1000