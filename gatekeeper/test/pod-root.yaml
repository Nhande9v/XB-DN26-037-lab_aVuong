apiVersion: v1
kind: Pod
metadata:
  name: bad-root
  namespace: demo
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: app
      image: nginx:1.27.0
      resources:
        limits:
          cpu: "100m"
          memory: "128Mi"